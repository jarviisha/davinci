---
"@jarviisha/davinci-react-ui": minor
---

Add a `transparent` option to `TableHeader`'s `tone`. It drops the header fill so the surface behind the table shows through, keeping the muted labels of the `default` tone. Additive and theme-aware like the other tones. Note: it isn't meant to be combined with `stickyHeader`, since the fill-free header lets scrolling body rows bleed through.
