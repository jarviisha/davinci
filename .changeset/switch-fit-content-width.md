---
"@jarviisha/davinci-react-ui": patch
---

Fix Switch clickable area stretching to full parent width inside flex/grid containers. The label is now sized to `fit-content` so the actionable region matches the track + label instead of the entire row.
